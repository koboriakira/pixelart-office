{
  "name": "pixelart-office",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --parallel --filter server --filter web dev",
    "lint": "pnpm --parallel --filter server --filter web lint",
    "test": "pnpm --parallel --filter server --filter web test",
    "build": "pnpm --parallel --filter server --filter web build"
  },
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  },
  "devDependencies": {
    "@eslint/js": "^9.39.3",
    "eslint": "^9.39.3",
    "typescript-eslint": "^8.56.1"
  }
}
